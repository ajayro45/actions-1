name: My First Workflow

on: 
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feature/*'

jobs: 
    first_job:
        runs-on: ubutnu-latest
        steps:
        - name: Welcome Mesage
          run: echo "My first Github Action Job"

        - name: List files
          run: ls

