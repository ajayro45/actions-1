name: My First Workflow

on: 
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feature/*'

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Mesage
          run: echo "My first Github Action Job"

        - name: List files
          run: ls

